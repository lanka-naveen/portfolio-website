{
  "name": "portfoliowebsite",
  "version": "1.0.0",
  "description": "Portfolio website backend",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^6.8.0",     // for MongoDB connection
    "body-parser": "^1.20.1", // for handling form data
    "cors": "^2.8.5"          // for enabling cross-origin requests
  }
}
